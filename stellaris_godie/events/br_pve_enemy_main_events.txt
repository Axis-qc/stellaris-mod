namespace = br

# ============================================================
# PVE刷敌调度入口：br.800
# 先排外圈 br.820-823，再排内圈 br.801-813
# ============================================================
country_event = {
    id = br.800
    hide_window = yes
    is_triggered_only = yes

    trigger = {
        has_global_flag = br_mode_pve
        NOT = { has_global_flag = br_pve_initialized }
        NOT = { has_global_flag = br_pve_spawn_started }
        exists = event_target:br_pve_enemy_country
    }

    immediate = {
        set_global_flag = br_pve_spawn_started
        country_event = { id = br.820 days = 3 }
    }
}