namespace = br
event = {
    id = br.1
    hide_window = yes

    is_triggered_only = yes
    trigger = {
        has_global_flag = yes_ship_all_add_2
    }

    immediate = {
        every_country = {
            add_modifier = { modifier = all_country_buff }
        }
    }
}