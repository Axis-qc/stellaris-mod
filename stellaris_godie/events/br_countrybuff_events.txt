namespace = br
event = {
    id = br.1
    hide_window = yes

    is_triggered_only = yes
    trigger = {
    }

    immediate = {
        every_country = {
            add_modifier = { modifier = all_country_buff }
        }
    }
}