# 利维坦·噬星者英雄舰方案 v2.2（核心机制已实现）

> **状态**：§1–§6 核心机制**已实现并实测通过**；获取方式**已由国民理念门控实现**（见 §6.5），原 §7 收复线方案**已废弃**。
> **实现文件索引见 §0**。本文件保留设计依据、拍板结论与踩坑记录；**具体实现以代码为准，不在此复制**。

---

## 〇、实现文件索引

| 设计章节 | 实现文件 |
|---|---|
| §2 光谱链、§5 效果 | `common/scripted_effects/lev_stellarite_effects.txt` |
| §4 技能组 | `common/scripted_actions/lev_stellarite_actions.txt` |
| §3 变量、技能回调事件 | `events/lev_stellarite_events.txt` |
| §6.2 舰种 | `common/ship_sizes/lev_stellarite_ships.txt` |
| §6.3 区段 | `common/section_templates/lev_stellarite_sections.txt` |
| §6 组件集 | `common/component_sets/lev_stellarite_component_sets.txt` |
| §6 核心组件 | `common/component_templates/lev_stellarite_cores.txt` |
| §6 星核（单档） | `common/component_templates/lev_stellarite_reactors.txt` |
| §6 能量喷射（单档） | `common/component_templates/lev_stellarite_thrusters.txt` |
| §6.4 神经接口 | `common/component_templates/lev_stellarite_computers.txt` |
| §6.4 舰船行为 | `common/ship_behaviors/lev_ship_behaviors.txt` |
| §5 光束数值 | `common/script_values/lev_stellarite_values.txt` |
| §5 外交修正 | `common/opinion_modifiers/lev_leviathan_opinions.txt` |
| §4 回调路由 | `common/on_actions/lev_leviathan_on_actions.txt` |
| §6.5 理念门控 | `common/governments/civics/lev_civics.txt`（civic key `lev_star_overlords`） |
| §6.5 开局选择窗、发放 | `events/lev_stellarite_events.txt`（`on_game_start_country` 挂 `lev.12`，`lev.10` 为隐藏发放） |
| §6.5 理念图标 | `gfx/interface/icons/governments/civics/lev_star_overlords.dds` |
| §6.5 理念本地化 | `localisation/simp_chinese/lev_civic_l_simp_chinese.yml` |
| 调试法令 | `common/edicts/lev_stellarite_debug_edicts.txt` |
| §8 本地化 | `localisation/simp_chinese/lev_stellarite_l_simp_chinese.yml` |
| §7 获取线 | **已废弃**（改由理念门控实现，见 §6.5） |

---

## 一、定位与原则

- **独立框架**：不复用 `heroship.*` / `br_vanguard` / `br_hero_*`，命名空间 `lev_*`，事件空间 `lev`。
- **数据落国家级变量**：不放在舰船本体，避免阵亡丢成长/能量。
- **文件独立**：所有 `lev_*` 一律新建独立文件，不往 `br_*` 文件里塞。

---

## 二、恒星光谱等级链

### 2.1 等级表（温度从高到低）

| 等级 | 光谱类 `sc_*` | 恒星行星类 `pc_*` |
|:--:|---|---|
| 7（顶） | `sc_b` | `pc_b_star` |
| 6 | `sc_a` | `pc_a_star` |
| 5 | `sc_f` | `pc_f_star` |
| 4 | `sc_g` | `pc_g_star` |
| 3 | `sc_k` | `pc_k_star` |
| 2 | `sc_m` | `pc_m_star` |
| 1（底） | `sc_m_giant` | `pc_m_giant_star` |

- **吸收 = 等级 −1**（向红端）；**重燃 = 等级 +1**（向蓝端）。
- **两者必须成对替换**：`solar_system = { set_star_class = sc_X }` 配 `change_pc = pc_X_star`。
  （`set_star_class` 是**星系作用域**；`change_pc` 直接作用在恒星行星上。）

### 2.2 边界与排除

| 规则 | trigger |
|---|---|
| 目标必须是恒星 | `is_star = yes` |
| 吸收下限 | `NOT = { is_star_class = sc_m_giant }` |
| 重燃上限 | `NOT = { is_star_class = sc_b }` |
| 多星系统排除 | `NOR = { is_binary_star = yes  is_trinary_star = yes }` |
| 特殊星体排除 | `NOR = { sc_black_hole  sc_neutron_star  sc_pulsar  sc_toxoid_star  sc_rift_star }` |
| 吸收自伤保护（D5） | 本国星系有殖民地时禁止吸收 |

> `is_star_class` 实际等价于 `is_system_class`（原版注释），在**星系**与**恒星行星**两种作用域都能用。

### 2.3 可选扩展（本期不做，登记备选）

| 项 | 说明 |
|---|---|
| 链尾补 `sc_t`（褐矮星，`pc_t_star`） | 吸收可再降一级逼近死星 |
| 吸干到底坍缩黑洞 | `sc_m_giant` 再吸收 → `sc_black_hole`，本星系殖民地全灭 + 全球外交惩罚 |
| 多星系统支持 | 需指定改哪一颗，且光谱语义混乱，建议永不做 |

---

## 三、核心资源（国家级变量）

| 变量 | 含义 | 规则 | 上限 |
|---|---|---|---|
| `lev_stellarite_energy` | 恒星能量 | 吸收 +1；重燃 −1；光束 −1 | 0 ~ 10 |
| `lev_growth` | 成长 | 吸收 +1；重燃 −1 | 0 ~ 10 |
| `lev_stellarite_unlocked` | 解锁 flag | 发放时置位，用于 `potential_country` | — |

**成长属性**（`triggered_ship_modifier`，`mult = owner.lev_growth`，与 `br_vanguard_auras.txt` 同构）：
每点 +5% 武器伤害 / +300 护甲 / +2% 船体再生。

> **坑**：`add_variable` / `change_variable` 无原生 clamp，必须用
> `if = { limit = { check_variable = { which = X value < 10 } } ... }` 包裹。

---

## 四、技能组

### 4.1 三个技能

| 技能 | slot | 释放 | 冷却 | 消耗 | 效果 |
|---|:--:|:--:|:--:|---|---|
| **吞噬恒星** | 1 | 30 天 | 360 天 | energy +1、growth +1 | 目标恒星光谱 −1；全星系宜居星降温 |
| **重燃恒星** | 2 | 30 天 | 360 天 | energy −1、growth −1 | 光谱 +1；全星系升温；冰封殖民地 → 盖亚 |
| **恒星光束** | 3 | 10 天 | 90 天 | energy −1 | 目标星系内**敌方**舰队受高额伤害 |

数值依据 `数值设计规范.md` §7.1：战略级（吸收/重燃）vs 战术级（光束）。

### 4.2 作用域（关键，文档 v2.1 此处有误）

```pdx
user_scope = fleet
scope = planet          # ← 不是 galactic_object
```

**恒星在引擎里是一个行星对象**（`is_star = yes`）。选 `planet` 的理由：

- 右键**恒星** → 走 planet 目标路径 ✓
- 右键**整个星系** → 引擎会向星系内**所有合法行星**排队（README `context_menu_ordering` 说明），用 `possible = { is_star = yes }` 一过滤即只剩恒星 ✓

> **一个动作覆盖两种右键姿势**，无需为每个作用域各写一份。

**作用域语义**（易错）：

| 块 | This | 国家引用 |
|---|---|---|
| `possible` | 目标恒星 | `from.owner`（From = 施放舰队） |
| `button_visible` / `button_clickable` | 舰队 | `owner` |

> **陷阱**：嵌套迭代块（`every_system_planet` / `every_fleet_in_system`）会把 `from` 链挤偏。
> 效果开头先 `from.owner = { save_event_target_as = lev_caster }`，之后统一用 event_target 引用。
> `solar_system = { }` 内取施放国用 `fromfrom.owner`（只多一层），再深一层就要 `fromfromfrom`。

### 4.3 回调

`on_completed = on_lev_*_completed` → on_action 路由 → **`planet_event`**（`system_event` 不适用）。

### 4.4 其他可用字段（v2.1 文档漏列）

| 字段 | 用途 |
|---|---|
| `finish_anim_state = "attack"` | 释放完成时播**非循环**动画。噬星者区段实体自带 `attack` 动画（混合 0.45） |
| `uses_vfx_type` | 执行时播 VFX，可选 `survey_effect` / `harvest_effect`（**待评估，本期未用**） |
| `on_started` / `on_progress_start` / `on_cancelled` / `on_queued` | 更细的生命周期钩子 |
| `cost = { energy = N }` | 直接声明资源消耗 |
| `completion_message_type` + `completion_message_localization` | 完成时通知（需用原版已有 toast 键） |
| `important` | 防误取消。**注意**：原版仅有的三个 `important = yes` 动作全部隐藏了右键菜单，慎用 |
| `shortcut` / `category` + `category_limit` | 快捷键 / 同目标并发限制 |

> `begin_anim_state` / `progress_anim_state` 原版只有巨构在用，对舰队有效性未知，本期未用。
> **坑**：`context_menu_ordering` 不要设成 `weight` —— 那会跳过 ai 权重 ≤ 0 的行星，而我们没写 `ai` block，结果是"排队了但一个都不选中"。默认 `distance` 正确。

### 4.5 按钮挂载

核心组件 `LEV_STELLARITE_CORE` 内声明 `scripted_action = { lev_stellarite_absorb / _ignite / _beam }`。

---

## 五、技能效果要点

实现见 `common/scripted_effects/lev_stellarite_effects.txt`。以下是**设计与踩坑要点**：

- **光谱改写**：见 §2.1，成对替换。
- **温度**：`spawn_planet_temperature_effect` + `decrease_/increase_planet_temperature`。每步自带 `reroll_planet` + `clear_blockers`，**副作用见附录 A.3**。
- **`stellarite_low_temp`** 复用原版静态修饰符：吸收时 `add_modifier`（`days = -1` 永久），重燃时 `remove_modifier`。
- **冰封救赎**：`pc_frozen` 且有殖民地 → `change_pc = pc_gaia`，**须在升温前执行**（否则 `pc_frozen` 先被升温链改掉）。
- **外交方向**（v2.1 文档写反）：
  `X = { add_opinion_modifier = { who = Y  modifier = Z } }` 改的是 **X 对 Y 的观感**。
  要让对方讨厌我们，必须在**对方 scope** 下执行、`who` 填**我们**。
- **光束伤害**（v2.1 公式有误）：
  `mult` 是**对累计值相乘**，`base=500 / add=成长 / mult=100` 算出来是 ≈5 万。
  正确写法是两层 script_value：`500 + 成长 × 100`（见 §14.1）。
- **光束敌我筛选**：`solar_system = { every_fleet_in_system = { limit = { controller = { is_hostile = lev_caster } } ... } }`。
  `damage_ship` 传入 `attacker` 不会断事件（已确认）。

---

## 六、舰船架构

### 6.1 文件

全部新建，见 §0 索引。**不碰**原版 `11_leviathans.txt` / `leviathans.txt`。

### 6.2 舰种要点

- `entity = "stellarite_entity"` —— 原版已有。它是**空 locator 网格**，只提供定位器；**可见模型完全来自区段实体**。（这是正常的两段式结构，不是缺陷。）
- 与原版守卫者 `stellarite` 的差异：

| 项 | 原版 | 本舰 |
|---|---|---|
| `is_space_object` | `yes` | **去掉** |
| `num_target_locators` | `0` | `4` |
| `is_designable` | `no` | `yes` |
| `max_hitpoints` | `200000` | `5000` |

- **[已实现] 不要设 `graphical_culture`**：设成 `{ "guardian_01" }` 会导致**舰船设计器里模型完全不渲染**（游戏内正常）。原版 stellarite 用 `no`，而它上一行 `#graphical_culture = { "guardian_01" }` 是**被注释掉的**（官方试过又放弃）。
  但 **`create_ship` 里要写** `graphical_culture = "guardian_01"`——不写会把玩家国家文化套到守卫者模型上，导致染色/光照错误。两者是两回事。
- `enable_default_design = yes` 必须开：`create_ship` / `random_existing_design` 依赖**已存在的设计**。
- **获取顺序不能颠倒**：先 `set_country_flag lev_stellarite_unlocked` → 再 `refresh_auto_generated_ship_designs` → 最后 `create_ship`。因为 `potential_country` 由该 flag 门控，标志未置位时自动设计里根本不会有噬星者。
- **必须自带反应堆与推进器**：原版同套组件靠 `potential`（如 `ship_uses_corvette_reactors`）限定舰种，自定义舰种装不上，会导致 `cannot build any component in the component set power_core` → 设计非法 → **刷船失败**。用 `size_restriction = { lev_stellarite }` 显式放开。
  （`combat_computers` / `sensor_components` / `ftl_components` 无此限制，可沿用原版。）
- **不挂人类科技前置，单档不升级**：噬星者是从恒星里长出来的生物，不会去"研发"裂变能 / 聚变能 / 零点能，也不会因为玩家点了 `tech_thrusters_2` 就换一具新引擎。因此：
  - 星核与能量喷射**均只保留 1 个组件**，无 `prerequisites`（不设 `potential = { have_technology }`）、无 `upgrades_to` 链；
  - 数值取原档位中段（星核 11000 出力 / 1600 合金；能量喷射 −200 出力 / 60 合金），保证设计与刷船不受玩家科技进度影响；
  - 拿到噬星者的瞬间即可用，强度稳定，也免去"开局刷船时玩家还没点出前置科技导致设计非法"的隐患。
  > 对照：先锋级（人类泰坦）走 6 档反应堆 + 5 档推进器并挂 `tech_*` 前置，那是**人类舰船**的做法，噬星者不沿用。
- **命名一律生物化**：这些组件的内核是普通舰船集（`power_core` / `thruster_components`），但噬星者是**生物巨兽**，中文名不能用人造舰的措辞。星核称**噬星者星核**（它吞食恒星物质后在体内炼成的能量器官），推进器称**噬星者能量喷射**（从星核抽取能量向身后喷出），作战电脑称**噬星者神经接口**（把巨兽的感知与意志接驳到指挥链上）。
  参照原版生物舰的命名法：`BIO_PROPULSION` = 生物推进器 / 离子基生物推进器、`BIO_SENSORS` = 重力调制感知器官、`BIO_ARMOR` = 纳米复合甲壳。
  > 原版生物舰（`07_space_monsters.txt`）其实走的是另一套组件集：`thruster_components_bio` / `sensor_components_bio` / `ftl_components_bio` / `neurochips`（**神经芯片**），且**没有反应堆这一项**。本舰沿用普通集是为了复用原版武器与供电体系，仅命名对齐生物风格。
  > 顺带：原版给它自己的生物舰反应堆取名 `BIO_FISSION_REACTOR` = **"裂变代谢"**、`BIO_FUSION_REACTOR` = "聚变代谢" —— 科技词 + 生物词，可作后续命名的参照。

### 6.3 区段槽位（17 槽）

布局照抄原版 `stellarite_key`，但**槽位模板必须换成 `invisible_*`**：

| 槽位 | 数量 | 模板 | 定位器 |
|---|:--:|---|---|
| `EXTRA_LARGE_GUN_01` | 1 | `invisible_extra_large_fixed` | `xl_gun_01`（`rotation = 180`） |
| `LARGE_GUN_01..08` | 8 | `invisible_large_turret_fixed` | `large_gun_01..08` |
| `PD_01..08` | 8 | `invisible_point_defence_turret` | `large_gun_01..05, 06, 06, 07`（**PD_06/PD_07 都指向 `large_gun_06`，原版笔误，照抄不修**） |
| 辅助槽 | 2 | `aux_utility_slots = 2` | — |

> **为什么必须用 `invisible_*`**：原版用的 `large_turret` / `point_defence_turret` **带 `entities` 块**，会渲染旋转炮塔 mesh。原版 stellarite 是 `is_space_object = yes` 的天体，玩家看不到；改成玩家舰后金属炮塔会长在生物模型上，很违和。
> `point_defence` 尺寸原版没有 fixed 变体（`is_fixed` 只覆盖 extra_large/large/medium/titanic/torpedo/planet_killer/engulf），但无 `entities` 就已经不渲染了。原版 `biogenesis_crisis.txt` 给生物型天灾舰也是这么用的。

### 6.4 舰船行为与作战电脑

- `common/ship_behaviors/lev_ship_behaviors.txt`：**`combat_movement = player_override` 是战斗中能手动放技能的关键**，缺了就无法手动释放。
- `common/component_templates/lev_stellarite_computers.txt`：唯一挂了该行为的作战电脑（`potential = { is_ship_size = lev_stellarite }`，`ai_weight = 999` 压过原版电脑）。中文名作**噬星者神经接口**——生物巨兽里不该出现"作战电脑"这种人造舰措辞。
- 参考：原版战列舰等作战电脑无法在战斗中接受指令，必须自带。

### 6.5 获取方式：国民理念门控

获取方式已由"收复线"改为**国民理念门控**（2026-09-12 拍板并实施）。

**理念**：`lev_star_overlords`，中文名随政体变化——星海之国（默认）/ 星海企业 / 星海蜂巢 / 星海机械。

| 项 | 实现 |
|---|---|
| 可选性 | `playable = { host_has_dlc = "Leviathans Story Pack" }`；`ai_playable = { always = no }`，`random_weight` / `ai_weight` 均为 0 |
| 伦理/政体限制 | 不设（`potential = { always = yes }`），四种整体共用同一份理念，靠 `swap_type` 按 `has_authority` 切换名称 |
| 锁定 | `modification = { add = { hidden_trigger = { NOT = { has_global_flag = game_started } } } remove = { always = no } }`——仅创建帝国时可添加，选后游戏内不可移除 |
| 效果 | `ship_military_cost_mult = 10`（军用舰船建造花费 +1000%）、`country_naval_cap_mult = -0.5`（海军容量 -50%） |
| 图标 | `gfx/interface/icons/governments/civics/lev_star_overlords.dds`（29x29 未压缩 32bpp，八边形，主色 `#00B38A`，符号色 `#162922`） |

**发放流程**：`on_game_start_country` → `lev.12`（可见选择窗，本期只有噬星者一项，为后续多利维坦预留）
→ 选项触发 `lev.10`（隐藏，门控 `has_civic = lev_star_overlords`）→ 置 flag / 刷设计 / 初始化变量
→ 1 天后 `lev.11` → `create_fleet` + `create_ship` 折跃至首都。

**两个都给**：既直接发一艘实船，也保留星港建造权限（限 1 艘，打沉可补）。

> **造价已下调**：`ship_military_cost_mult` 按 `shipclass_military` 判定，噬星者自身也属于该类，会被一起涨 10 倍。
> 因此基础造价由 1500 下调至 **150 合金**，×10 后仍是 1500，补船成本与改造前一致。

**已实测通过（2026-09-12）**：`swap_type` 的 `name` 字段确实能换名；蜂巢 / 机械 / 企业都能在帝国创建界面看到该理念；`add` 的 `game_started` 条件生效。
> 副作用：旧存档无法使用该理念（`game_started` 已为 true，且开局事件早已过去），需开新档。
> 用户对此的确认：利维坦成长需要时间，中途加入只能拿到未成长的形态，本来就没有意义。

---

## 七、获取线（收复线）—— 已废弃

> **2026-09-12 废弃**：获取方式已改为国民理念门控，见 §6.5。本节仅作历史记录保留，不再实施。
>
> 思路差异：原设计把获取拆成"探索发现异常读数 → 进入遭遇 → 驯化项目 → 收复或失败"四阶段，属于散布在探索流程里的随机奖励；最终方案把选择权前置到帝国创建阶段（选理念即解锁），让整条利维坦线成为一个独立的玩法流派，而不是探索途中的彩蛋。

原设计（未实施，仅存档）：

与现有先锋级发放流程完全独立，`lev` 命名空间：

```
Phase 0 信号（lev.1）  ：探索时发现「异常引力读数」→ 每局随机 1 个目标星系
Phase 1 接触（lev.2）  ：舰队进入 → 遭遇恒星吞噬者（中立，可退出）
Phase 2 收复项目        ：特殊项目「驯化噬星者」——需科技 + 资源 + 时间（可失败）
Phase 3 获得（lev.10）  ：成功 → 置 flag lev_stellarite_unlocked + 发放舰船
失败分支                ：项目失败 → 利维坦转为敌意，击杀后掉战利品（不发放）
```

### 7.1 与原版守卫者据点的隔离

原版**自带**一个恒星吞噬者据点星系（`solar_system_initializers/leviathans_system_initializers.txt:1493`，`max_instances = 1`，flag `guardians_stellarite_system`），是一条完整独立的守卫者线。

**已拍板**：**独立新星系，不碰原版。**

| 项 | 决定 |
|---|---|
| 己方据点星系 | 新写 `common/solar_system_initializers/lev_stellarite_initializers.txt`，用专属 flag `lev_stellarite_home_system` |
| 原版守卫者 | 原样保留，互不干扰 |
| 玩家收复线 | 只认 `lev_stellarite_home_system` flag，与原版零耦合 |
| DLC 门槛 | 两线都在 `has_leviathans = yes` 之下，但彼此独立判定 |

> **为什么不复用**：复用要改 `guardians_init_stellarites` 或抢占它的 `max_instances = 1` 名额，会连带影响原版守卫者 AI、奖励掉落与 `antique_threat_system` 计数。隔离成本远低于耦合成本。

### 7.2 守卫者排除清单

原版 `common/scripted_triggers/02_scripted_triggers_enclaves.txt:22` 已列全守卫者/利维坦 ship_size。凡是要筛"玩家舰/守卫者舰"的地方**照抄这份清单**，别自己猜。

> 注意：我们新建的是 `lev_stellarite`，**不在**这份清单里——这是选独立 ship_size 名的额外好处。若后续要给 `lev_stellarite` 写"是否守卫者"判定，**必须显式排除自己**。

---

## 八、本地化

以 `localisation/simp_chinese/lev_stellarite_l_simp_chinese.yml` 为准（**UTF-8 BOM**）。
覆盖：事件、技能按钮与失败提示、舰队 UI 活动串、变量动态 tooltip、船/区段/组件/作战电脑/舰船行为、外交修正、调试法令。

**数值显示**：技能按钮 tooltip 采用先锋舰同构格式——`§G技能名§!` + 效果与数值 + 冷却，**不罗列字段**（避免写成参数清单）。
动态数值走 `common/scripted_loc/lev_stellarite_loc.txt` 的 `defined_text`（`value = value:<script_value>`）：

| defined_text | 显示内容 |
|---|---|
| `lev_beam_damage_display` | 光束单次伤害（随成长实时变化） |

> 当前能量/成长值直接引用变量：`[Root.owner.lev_stellarite_energy]` / `[Root.owner.lev_growth]`（舰队 action tooltip 作用域，`owner` = 施放国）。
> 成长的加成公式写在变量 `lev_growth_desc` 中，不重复塞进三个技能 tooltip。
> **坑**：法令的本地化键是 `edict_<key>` 与 `edict_<key>_desc`，不是 `<key>`。

---

## 九、号段分配（防冲突）

| 资源 | 已占用 | 本方案分配 |
|---|---|---|
| 事件命名空间 | `br` / `brstart` / `heroship` / `reward` / 原版 `leviathans` | **`lev`** |
| 事件号段 | — | `lev.1–99` 获取线 ／ `lev.100–199` 技能 ／ `lev.200+` 后续利维坦 |
| on_action | `on_br_*` | `on_lev_absorb_completed` / `_ignite_` / `_beam_` |
| 变量 | `br_*` | `lev_stellarite_energy` / `lev_growth` |
| flag | `br_*` | `lev_stellarite_unlocked` / `lev_stellarite_*` |
| 本地化 | `BR_*` | `LEV_*` / `lev_*` |

---

## 十、实现状态

| 步骤 | 内容 | 状态 |
|:--:|---|---|
| 1 | `lev_stellarite` ship_size + 区段克隆 | [已实现] |
| 2 | 核心组件 + 3 个 `scripted_action` + on_action 路由 | [已实现] |
| 3 | 光谱改写 + 温度 + 外交 | [已实现] |
| 4 | 光束伤害 + `script_value` 缩放 | [已实现] |
| 5 | 自带星核 / 能量喷射 / 神经接口 / 舰船行为 | [已实现] |
| 6 | 国民理念门控 `lev_star_overlords`（含图标） | [已实现] 实测通过 |
| 7 | 开局选择窗 `lev.12` 替代直接发放 | [已实现] 实测通过 |
| 8 | 本地化（BOM）+ 变量动态 tooltip | [已实现] |
| 9 | 游戏内联调 | [已实现] 实测通过 |
| 10 | ~~获取线（§7）~~ | **[已废弃]** 改由理念门控实现 |

---

## 十一、剩余风险项

> `damage_ship` 传 `attacker` 不会断事件、改恒星类的存档兼容——**均不在考虑范围**（已确认）。

| # | 风险 | 验证方式 |
|:--:|---|---|
| 1 | 克隆 ship_size + 引用原版 `stellarite_entity` 后模型/动画 | 已验证通过 |
| 2 | 原版 stellarite 的区段槽位定位器对**玩家武器**是否正常挂载 | 装配 XL/L 武器实测 |
| 3 | `planet` 作用域的舰队动作在非战斗场景（行星际航行 + 释放计时）是否顺畅 | 已验证通过 |
| 4 | 大星系性能（15+ 行星，每层遍历 `every_system_planet` 两次） | 找一个 15+ 行星星系跑一遍 |
| 5 | 温度 effect 的 `reroll_planet` 副作用（重燃自家已开发星球体验） | 见附录 A.3 |
| 6 | 获取线的独立初始星系与原版守卫者是否互相挤占刷新名额 | §7 实现时验证 |

---

## 十二、待拍板决策（历史追溯，均已拍板）

> FYI：已于 2026-09-10 全部拍板 → 最终取值见 §14。此表保留原始提议与备选。

| # | 决策 | 推荐 | 备选 |
|:--:|---|---|---|
| **D1** | 能量 / 成长上限 | 各 10 | 20（偏强） |
| **D2** | 成长斜率 | 每点 +5% 伤害 / +300 护甲 / +2% 再生 | 纯百分比 / 纯绝对值 |
| **D3** | 冷却与释放 | 吸收·重燃 30/360；光束 10/90 | 吸收重燃降到 240 天 |
| **D4** | 船体与造价 | 船体 5000 / 1500 合金 | 3500 ／ 8000 |
| **D5** | 施放范围 | 吸收：本国/盟友有殖民地时禁止，他国星系可执行（外交 −100）；重燃：任意星系 | 吸收也不限制自家 |
| **D6** | 链尾扩展 | V1 不做 | 褐矮星 / 吸干坍缩黑洞 |

---

## 十三、变更清单

### 13.1 v1 → v2

| 项 | v1 | v2 |
|---|---|---|
| 技能释放 | special project + 每日 tick CD 变量 | `scripted_action`（原生 `required_progress` + `cooldown`） |
| 技能目标 | 利维坦当前所在星系 | 玩家自选 |
| CD 变量 | 三个 | 删除 |
| 恒星改写 | 未确定 | `set_star_class` + `change_pc` 成对 |
| 基座实体 | "克隆时补写" | 直接引用原版 `stellarite_entity` |
| 伤害技能 | 未确定 | `damage_ship` |

### 13.2 v2 → v2.1

| 项 | v2 | v2.1 |
|---|---|---|
| 区段槽位 | 1 XL + 8 L + 3 PD + 4 辅助 | **1 XL + 8 L + 8 PD + `aux_utility_slots = 2`** |
| 获取线星系 | 只说"随机 1 个目标星系" | 独立新星系 `lev_stellarite_home_system` |
| 温度反馈 | 仅改 pc | 补 `stellarite_low_temp` 复用 |
| 边界前置块 | 自拟 | 照抄原版 `cosmic_storms_events_1.txt:4530` |

### 13.3 v2.1 → v2.2（本次：文档与实现对齐）

| 项 | v2.1 | v2.2 |
|---|---|---|
| 作用域 | `galactic_object`（回调 `system_event`） | **`planet`（回调 `planet_event`）**，同时支持右键恒星与右键星系 |
| `graphical_culture` | "要去掉"（**错**） | **不要设**（设了设计器不渲染）；但 `create_ship` 里要写 `guardian_01` |
| 光束伤害公式 | `base=500 / add=成长 / mult=100`（**算出来 ≈5 万**） | 两层 script_value 实现 `500 + 成长×100` |
| 外交方向 | 我方 scope 下 `who = 对方`（**反了**） | 对方 scope 下 `who = 我们` |
| `possible` 国家引用 | `owner`（**错**） | `from.owner` |
| 技能字段 | 仅基础字段 | 补 `finish_anim_state`；登记 `uses_vfx_type` 等待评估字段 |
| 组件文件 | 13 个 | **+4**：星核、能量喷射、神经接口、舰船行为 |
| 开局发放 | 未定 | 所有人类玩家直接发（临时方案），DLC 门槛 |
| 文档形态 | 含代码骨架 | **已实现部分改为文件索引**（§0），代码不复制 |

---

## 十四、D1–D6 拍板结论（2026-09-10，最终）

| # | 决策 | **最终取值** | 理由 |
|:--:|---|---|---|
| **D1** | 能量 / 成长上限 | **各 10** | +5%/点 × 10 = +50%，稳在「单技能 ≤5× 原版」之下 |
| **D2** | 成长斜率 | **每点 +5% 武器伤害 / +300 护甲 / +2% 船体再生** | 与现有英雄舰同构，零维护 |
| **D3** | 冷却与释放 | **吸收·重燃：30 / 360；光束：10 / 90** | 对齐 `数值设计规范.md` §7.1（战略级 vs 战术级） |
| **D4** | 船体与造价 | **船体 5000 / 造价 1500 合金** | 比先锋级 3000 厚一档，符合"巨型生物"体感 |
| **D5** | 施放范围 | **吸收**：本国星系有殖民地时禁止；他国星系可执行，外交 −100。**重燃**：任意星系 | 形成闭环：吸别人的星充能 → 重燃救自己/盟友的冰封星 |
| **D6** | 链尾扩展 | **本期不做** | 先让主线跑通 |

### 14.1 光束伤害数值（v2.2 修正）

**单次伤害 = 500 + 成长 × 100**（`growth=0 → 500`；`growth=10 → 1500`）

> [修正] v2.1 写的 `base = 500 / add = owner.lev_growth / mult = 100` 是错的——
> `mult` 对**累计值**相乘，实际结果是 `(500 + 成长) × 100 ≈ 5 万`。
> 正确写法：两层 script_value（外层 `base = 500` + `add = value:<每点小计>`；内层 `base = 0` + `add = owner.lev_growth` + `mult = 100`）。见实现文件。

| 状态 | 单次伤害 | 说明 |
|---|---|---|
| growth = 0 | 500 | 打不掉满编战列，能清护卫/驱逐 |
| growth = 10 | 1500 | 「战术支援」级，**仍不是一键清场**，且只对目标星系敌方舰队生效 |

> 不用"百分比掉血"：会让它对 Boss/PVE 刷怪也无差别超模。若实测偏弱，优先调 `base`，不动 `mult`。

---

## 十五、实测清单（含已记录的坑）

| # | 项 | 状态 |
|:--:|---|---|
| 1 | 克隆后模型/动画是否正常 | 已验证通过 |
| 2 | 自带星核/能量喷射解决 `cannot build any component` | 已验证通过（档位精简后待复测） |
| 3 | `damage_ship` 是否断事件 | 传 `attacker` 后正常，非风险 |
| 4 | `graphical_culture` 是否影响设计器渲染 | 已验证：设了会导致空白 |
| 5 | 技能右键菜单两条路径 | 已验证通过 |
| 6 | `is_binary_star` 在行星作用域下是否成立 | 已验证通过 |
| 7 | 大星系性能 | 待测 |
| 8 | 本地化文件 UTF-8 BOM | 已验证（`efbbbf`） |
| 9 | 星核 / 能量喷射精简为单档后能否正常刷船与设计 | 待测 |

---

## 附录 A、原版温度阶梯映射（含副作用）

> 出处：`common/scripted_effects/00_scripted_effects.txt`（`increase_planet_temperature` 第 179 行 / `decrease_planet_temperature` 第 255 行）

### A.1 升温链（由冷到热）

| 链 | 阶梯 |
|---|---|
| 湿链 | `pc_frozen → pc_arctic → pc_ocean → pc_desert → pc_molten` |
| 干链 | `pc_barren_cold → pc_tundra → pc_tropical → pc_savannah → pc_barren → pc_molten` |
| 高地链 | `pc_alpine → pc_continental → pc_arid → pc_barren → pc_molten` |

### A.2 降温链（由热到冷）

| 链 | 阶梯 |
|---|---|
| 主流 | `pc_molten → pc_desert → pc_ocean → pc_arctic → pc_frozen` |
| 支流 | `pc_barren → pc_arid → pc_continental → pc_alpine → pc_frozen` |
| 旁支 | `pc_savannah → pc_tropical → pc_tundra →` **随机** `pc_frozen` / `pc_barren_cold` |

### A.3 [注意] 副作用（直接影响 D5）

每一步都执行 **`reroll_planet = yes` + `clear_blockers = yes`**：

- 会**重随机数星球 modifier 与地块障碍** → 玩家自家已开发的殖民地被「重燃」洗一遍，deposits / 地形很可能变样；
- **`pc_gaia` 不在任何一条链里** → 盖亚星不参与温度改写，是天然的「安全终点」（正好呼应 §5 的 `pc_frozen → pc_gaia`）；
- 到 `pc_frozen` / `pc_barren_cold` / `pc_molten` 后再调用不再变（没有匹配的 `else_if`，静默无效）——**星球温度边界由原版自带，我们不需要自己写 climate 边界**，只需在 UI 上提示。

> 因此 D5 附带一条提醒：重燃虽然不加限制，但**作用于本国已有殖民地时，UI 应提示「此操作会重塑地表（清空地块障碍并重新随机地貌）」**，否则会出现"我只是想救冰封星球，结果把首都洗了"的负反馈。

### A.4 改星的两种原版写法

```pdx
# 写法一：anomaly_events_4.txt:4985-4993 —— 先改星系，再筛 is_star 的主星改 pc
solar_system = {
  set_star_class = sc_m
  every_system_planet = { limit = { is_star = yes } change_pc = pc_m_star }
}

# 写法二：cosmic_storms_events_1.txt:4530-4595 —— 主星 pc 先改，再跳到星系改光谱
random_list = {
  20 = { change_pc = pc_a_star  solar_system = { set_star_class = sc_a } }
}
```

**本方案采用写法一的思路**（`set_star_class` 在星系作用域执行，`change_pc` 直接作用于恒星行星）。

三点已核实结论：

1. `is_binary_star` / `is_trinary_star` 是脚本触发器，可直接写在效果/触发器作用域里。
2. 原版**没有**排除 `sc_neutron_star` 与 `sc_pulsar`（其中 neutron 被当作合法目标）。**我们的方案比原版更保守**（把 neutron / pulsar 也排除）——有意的：玩家技能不该制造脉冲星。
3. `random_list` 的键是**权重值**（`20 = {...}`），不是 `base` / `factor`。

---

## 附录 B、原版机制核实结论

### B.1 守卫者模型改玩家舰的渲染要点

| # | 项 | 结论 |
|:--:|---|---|
| 1 | `graphical_culture` | **ship_size 上不要设**（`guardian_01` 会让设计器不渲染模型）；但 **`create_ship` 里要写** `guardian_01` |
| 2 | 槽位模板 | 用不带 `entities` 的 `invisible_*`，否则会渲染旋转炮塔 mesh |
| 3 | 模型结构 | 两段式：ship_size 挂空 locator 网格，**可见模型完全由区段实体提供** |

### B.2 自定义舰种的组件限制

- **必须自带** `power_core` / `thruster_components` 组件（原版靠 `potential` 限定舰种，自定义舰种装不上）。
| `combat_computers` / `sensor_components` / `ftl_components` 无此限制，可沿用原版。
- **作战电脑必须自带**：`combat_movement = player_override` 是战斗中手动放技能的前提。
- 星核 / 能量喷射**不设科技前置、不做档位**：生物不沿人类科技树成长，只保留 1 个组件，拿到即用。

### B.3 其他核实项

| 项 | 结论 |
|---|---|
| `stellarite_entity` / `stellarite_ship_section_entity` | 均存在于 `gfx/models/ships/guardians/stellarite/stellarite.asset` |
| `scripted_action` 的 `scope = planet` | 回调事件类型为 `planet_event` |
| `required_progress` 单位 | tick 数，最小 2.0 才非瞬时 |
| `cooldown` 单位 | 天 |
| `is_star_class` | 实际等价于 `is_system_class`，星系与恒星行星作用域都可用 |
| `add_opinion_modifier` | `X = { add_opinion_modifier = { who = Y } }` 改的是 **X 对 Y** 的观感 |
| `script_value` 的 `mult` | 对**累计值**相乘（在 `add` 之后） |
| `lev` 命名空间 | 原版仅占用 `leviathans`，`lev` 空置 |
| `stellarite_low_temp` | 原版静态修饰符，可复用 |
| 噬星者模型动画 | `stellarite_ship_section_entity` 有 5 个状态 / 3 个动画（`idle` / `death` / **`attack`**），`attack` 非循环、混合 0.45 |
| 舰船模型两段式 | ship_size 实体是空 locator 网格，可见模型来自区段实体 |
