# 航母核心（Carrier）— 舰载机 / 编队制空 / 多舰种协同向

> **✅ 已实现，2026-06-02 技能重做。2026-06-03 大招重做。**

## 核心定位

航母核心专注于**无人机作战与制空权争夺**。选航母就是选"掌控天空"——无人机舰队的规模直接受英雄等级影响，通过技能维持空中优势，同时通过射程光环支援主力舰队。

## 关键实现约束

英雄舰（`br_vanguard`）**无法合并外来舰船**到自己的舰队中，所有召唤单位以独立舰队形式在星系内协同作战。

## 无人机设计（当前方案）

航母核心召唤的无人机 **直接复制玩家当前的护卫舰设计**（`design = NAME_Dagger`），使用玩家已解锁的科技和武器，相当于额外的免费护卫舰。

```
create_ship = { name = random design = NAME_Dagger }
```

> **未来可升级**：后续可新增自定义 `br_carrier_drone` ship_size，替换 `NAME_Dagger` 以获得独立数值控制和专属外观。

## 核心机制一：航母常驻舰队

航母核心拥有 **专属常驻舰队**（标记 `br_carrier_fleet`），在第一次使用技能时创建，此后所有技能只往这支舰队里补船，不清空、不销毁。舰船在战斗中自然损失，技能负责补充。

## 核心机制二：无人机数量由英雄舰决定

无人机舰队的总上限直接取决于英雄等级，在技能事件中用变量计算：

```
# 在技能事件中
set_variable = { which = br_carrier_cap value = owner.br_hero_level }
multiply_variable = { which = br_carrier_cap value = 2 }    # 上限 = 英雄等级 × 2
```

| 英雄等级 | 无人机上限 |
|:-------:|:---------:|
| Lv2 | 4 艘 |
| Lv6 | 12 艘 |
| Lv12 | 24 艘 |

所有补船技能只需检查当前舰队 **缺多少补多少**，用 `br_carrier_cap` 变量作上限比对。

## 核心机制三：射程光环（被动）

选择被动 **④ 制空权** 后激活。根据星系内无人机数量，给所有友军舰船加射程：

| 无人机数量 | 射程加成 | 战术含义 |
|:---------:|:--------:|---------|
| 5 艘 | +5% | 小规模编队，略有优势 |
| 10 艘 | +10% | 满编时最大化优势 |

加成受 `sk2_mult` 倍率强化，月度脉冲由 1335 刷新。

**不选此被动则无射程光环**，无人机只提供火力输出。

---

## 技能选择

* Lv2（主动×2选1）：**编队出击 / 无人机增强**
* Lv4（被动×2选1）：**甲板维护 / 制空权**
* Lv6（大招×2选1）：**指挥链路 / 全甲板突击**

---

## 主动技能（Lv2 二选一）

### ① 编队出击 — 应急补充

向常驻航母舰队补充 `hero_level` 艘无人机，**可超上限**。

* 补充数量等于**当前英雄等级**（Lv2=2艘，Lv6=6艘，Lv12=12艘）
* 可超出 `br_carrier_cap` 上限——超出的船不会消失，但月度自动补船仍只补到上限
* 冷却 **120 天**（受 `cdmult` 强化）

**设计意图**：快速爆兵应对突发战损。超上限的额外船是临时优势，后续靠自然战损回落到常规水平。

**实现方式**：利用现有 1331→1332→1333 月度链，设 `br_carrier_full_replenish` flag，并在 1332 中将 `br_carrier_deficit` 设为 `hero_level`（而不是 `cap - count`）。

---

### ② 无人机增强 — 火控强化

给常驻无人机舰队施加 **命中 + 追踪** buff：

* 全队 `ship_accuracy_add` + **15 × sk1_mult**
* 全队 `ship_tracking_add` + **15 × sk1_mult**
* 持续 **20 天**（受 `daymult` 强化）
* 冷却 **180 天**（受 `cdmult` 强化）

**实现方式**：`fleet_event` → `event_target:br_void_country` → `random_owned_fleet`（`has_fleet_flag = br_carrier_fleet_@root`）→ `add_modifier`：

```
add_modifier = {
    modifier = br_carrier_drone_accuracy
    mult = root.owner.value:br_skill_1_mult
    days = 1
    time_multiplier = root.owner.value:br_skill_1_daymult
}
```

---

## 被动技能（Lv4 二选一，可切换）

通过英雄舰 UI 随时切换当前激活的被动技能。切换时自动交换对应的 `_unlocked` 标记。

### ③ 甲板维护（Lv4）— 加速补给

**月度补给速率**提升：

* 基础每月补 **1** 艘
* 每级英雄等级额外 +**1** 艘（Lv4=4艘/月，Lv12=12艘/月）
* 受 `sk2_mult` 倍率强化

**不选此被动则月度只补 1 艘**。

**实现方式**：在 1332 的补给速率计算中，检查 `br_skill_deck_maintenance_unlocked`，将 `br_carrier_supply_count` 改为 `hero_level`（而非旧版的固定+1/+2）。

---

### ④ 制空权（Lv4）— 射程光环

根据星系内 **无人机数量** 给同星系 **友军舰船** 加射程：

* 每有 **5 艘无人机**，友舰 `ship_weapon_range_add` **+5%**
* 上限：**10 艘无人机 = +10%** 射程
* 受 `sk2_mult` 倍率强化
* 月度脉冲刷新

**设计意图**：不选此被动则无人机只提供火力输出，选它后无人机本身也成为战术资产——拉开阵型的越多，全队射程越远。

**实现方式**：在 1335（原制空权位置）中，直接读 `br_carrier_count` 变量，计算阶梯：

```
event_target:br_hero_system_@root = {
    every_fleet_in_system = {
        limit = {
            exists = owner
            owner = { OR = { is_same_value = root is_country_type = br_player_pve } }
        }
        add_modifier = {
            modifier = br_carrier_range_aura
            mult = root.value:br_carrier_range_mult
            days = 32
        }
    }
}
```

---

## 大招（Lv6 二选一）

两个大招形成 **质 vs 量** 的哲学对立——⑤ 牺牲英雄舰自身换取无人机精锐化，⑥ 以绝对数量淹没敌人。

### ⑤ 指挥链路 — 能量分散

英雄舰 **临时削弱自身**，将抽取的属性按无人机数量 **成倍赋予常驻无人机舰队**。转移量由当前 `br_carrier_count` 动态决定。

**转移公式**：

```
transfer_count = min(br_carrier_count, 12)    # 上限 12 架，防止英雄舰伤害跌破 -1.0
drone_mult     = transfer_count × sk3_mult    # 无人机获得技能倍率放大
```

| 属性 | modifier 字段 | 每架英雄舰损失 | 每架无人机获得（sk3 前） |
|:---|:---|:---:|:---:|
| 伤害 | `ship_damage_mult` | **-0.04** | +0.04 |
| 命中 | `ship_accuracy_add` | **-6** | +6 |
| 追踪 | `ship_tracking_add` | **-6** | +6 |

**战术场景**：

| 场景 | 无人机数 | 英雄舰损失 | 无人机获得（sk3=2.0） |
|:---|:---:|:---|:---|
| 刚开战 | 6 架 | 伤害 -24%, 命中 -36, 追踪 -36 | 伤害 +48%, 命中 +72, 追踪 +72 |
| 上限 | 12 架 | 伤害 -48%, 命中 -72, 追踪 -72 | 伤害 +96%, 命中 +144, 追踪 +144 |
| 残编 | 3 架 | 伤害 -12%, 命中 -18, 追踪 -18 | 伤害 +24%, 命中 +36, 追踪 +36 |

**参数**：

| 参数 | 值 | 说明 |
|:---|:---|------|
| 转移计数上限 | **12** | 防止 `ship_damage_mult` 跌破 -1.0 |
| 持续时间 | **45 天**（受 `daymult` 强化） | 比旧版更长，代价是英雄舰变废 |
| 冷却 | **540 天**（受 `cdmult` 强化） | 最长大招 CD |

**设计意图**：无人机越多，转移效果越强，但英雄舰也越脆弱——玩家需要判断"值不值得"。适合防守战、阵地战。

**实现方式**：`fleet_event` → 读 `br_carrier_count` → 计算 `transfer_count` 和 `drone_mult` → 分别对英雄舰和无人机施加 modifier：

```
# 英雄舰自损（每架值，实际由 transfer_count 放大）
br_carrier_command_link_self = {
    ship_damage_mult   = -0.04
    ship_accuracy_add  = -6
    ship_tracking_add  = -6
}

# 无人机增益（每架值，实际由 drone_mult = command_link_count × sk3_mult 放大）
br_carrier_command_link_drone = {
    ship_damage_mult   = 0.04
    ship_accuracy_add  = 6
    ship_tracking_add  = 6
}
```

---

### ⑥ 全甲板突击 — 遮天蔽日

召唤一支 **独立临时舰队**，纯数量碾压，不附带任何质量 buff。

| 参数 | 值 | 说明 |
|:---|:---|------|
| 召唤数量 | `hero_level × 6` | Lv6=36艘，Lv12=72艘 |
| 持续时间 | **45 天**（受 `daymult` 强化） | 数量多故缩短 |
| 冷却 | **420 天**（受 `cdmult` 强化） | |
| 额外效果 | **无** | 纯数量，不附带伤害/命中 buff |

**设计意图**：和⑤属性转移形成对位——⑤是质变（少量变精锐），⑥是量变（爆发式数量）。不依赖现有无人机，适合进攻开战、清图。临时舰队到期自毁。

**实现方式**：与旧 1320 结构一致，去掉 `add_modifier` 行，`while count` 改为 `hero_level × 6`，临时舰队到期自毁机制完全复用：

```
create_fleet = {
    name = random
    settings = { ... }
    effect = {
        set_owner = event_target:br_void_country
        set_location = { target = event_target:br_hero_ship_@root }
        while = {
            count = root.owner.value:br_carrier_mass_summon_count
            create_ship = {
                name = random
                design = event_target:br_carrier_ship_@root
            }
        }
        set_timed_fleet_flag = { flag = br_carrier_strike_fleet days = 45 }
    }
}
```

> 注意：临时舰队设 `owner = br_void_country`，与主舰队的归属一致。

---

### 大招对比

|| ⑤ 指挥链路 | ⑥ 全甲板突击 |
|:---|:---|:---|
| 哲学 | **质** — 牺牲自己，强化部队 | **量** — 绝对数量淹没敌人 |
| 依赖 | 需要已有无人机（数量决定强度） | 不依赖现有无人机 |
| 英雄舰 | 大幅削弱（代价） | 无影响 |
| 无人机 | 质变（少量变精锐） | 量变（爆发式数量） |
| 最佳时机 | 防守战、阵地战 | 进攻开战、清图 |
| 风险 | 英雄舰被集火暴毙 | 临时舰队被 AOE 清场 |

---

## 实现文件清单

本次大招重做涉及的变更文件：

| # | 文件 | 操作 |
|:-:|------|------|
| 1 | `events/heroship_skill_carrier_events.txt` | **重写** 1320（指挥链路）、1321（全甲板突击）事件逻辑 |
| 2 | `common/static_modifiers/br_countrybuff.txt` | **新增** `br_carrier_transfer_self`、`br_carrier_transfer_drone`；**废弃** 旧 `br_carrier_full_deck`、`br_carrier_swarm_*`、`br_carrier_hero_empower` |
| 3 | `localisation/simp_chinese/br_hero_ship_l_simp_chinese.yml` | **修改** Lv6 技能名、描述 |
| 4 | `events/heroship_level_events.txt` | **修改** Lv6 技能选择描述文本 |

### 技能变量对照（废弃 → 新增）

| 旧变量/flag | 新变量/flag | 用途 |
|:---|:---|------|
| `br_carrier_full_deck_dmg` | — | 已废弃，新⑥无质量 buff |
| `br_carrier_full_deck_day` | `br_carrier_mass_summon_day` | 新⑥持续时间 |
| `br_carrier_full_deck_cd` | `br_carrier_mass_summon_cd` | 新⑥冷却 |
| `br_carrier_swarm_*` | `br_carrier_command_link_*` | 新⑤指挥链路 |
| `br_carrier_hero_empower*` | — | 已废弃，由指挥链路取代 |
| — | `br_carrier_command_link_count` | 转移计数（min(count,12)） |
| — | `br_carrier_command_link_drone_mult` | 无人机倍率（count × sk3_mult） |
