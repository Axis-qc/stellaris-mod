# 英雄舰技能切换按钮 — 国家作用域
# allow 查对方技能的 _unlocked flag，点过一次后两边互通
# ── 主动技能 slot 1 ──
br_level_ui_active_slot1 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			#条件不满足时显示文本
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 2
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 2
						}
					}
					NOT = {
						has_country_flag = br_skill_active_slot_1
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_active_slot_2
				set_country_flag = br_skill_active_slot_1
			}
		}
	}
}

# ── 主动技能 slot 2 ──
br_level_ui_active_slot2 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 2
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 2
						}
					}
					NOT = {
						has_country_flag = br_skill_active_slot_2
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_active_slot_1
				set_country_flag = br_skill_active_slot_2
			}
		}
	}
}

# ── 被动技能 slot 1 ──
br_level_ui_passive_slot1 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 4
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 4
						}
					}
					NOT = {
						has_country_flag = br_skill_passive_slot_1
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_passive_slot_2
				set_country_flag = br_skill_passive_slot_1
			}
		}
	}
}

# ── 被动技能 slot 2 ──
br_level_ui_passive_slot2 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 4
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 4
						}
					}
					NOT = {
						has_country_flag = br_skill_passive_slot_2
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_passive_slot_1
				set_country_flag = br_skill_passive_slot_2
			}
		}
	}
}

# ── 旗舰超载 slot 1 ──
br_level_ui_ultimate_slot1 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 6
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 6
						}
					}
					NOT = {
						has_country_flag = br_skill_ultimate_slot_1
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_ultimate_slot_2
				set_country_flag = br_skill_ultimate_slot_1
			}
		}
	}
}

# ── 旗舰超载 slot 2 ──
br_level_ui_ultimate_slot2 = {
	potential = {
		from = {
			check_variable = {
				which = br_level_ui_tab
				value = 1
			}
		}
	}
	allow = {
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_LOCKED
			from = {
				check_variable = {
					which = br_hero_level
					value >= 6
				}
			}
		}
		custom_tooltip = {
			fail_text = BR_LEVEL_UI_SKILL_FAIL_TT
			from = {
				OR = {
					NOT = {
						check_variable = {
							which = br_hero_level
							value >= 6
						}
					}
					NOT = {
						has_country_flag = br_skill_ultimate_slot_2
					}
				}
			}
		}
	}
	effect = {
		hidden_effect = {
			from = {
				remove_country_flag = br_skill_ultimate_slot_1
				set_country_flag = br_skill_ultimate_slot_2
			}
		}
	}
}
