# 测试法令 — 直接切换英雄舰技能 flag
# 激活即生效，length = 1 瞬间结束，可反复点击切换。
# 以下法令仅在 br_debug_mode 激活时可见。

# 全局调试开关 — 始终可见，用于打开/关闭调试模式
br_test_toggle_debug = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { always = yes }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_debug_mode }
            remove_country_flag = br_debug_mode
        }
        else = {
            set_country_flag = br_debug_mode
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_self_repair = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_self_repair_unlocked }
            remove_country_flag = br_skill_self_repair_unlocked
        }
        else = {
            set_country_flag = br_skill_self_repair_unlocked
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_combat_stack = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_combat_stack_unlocked }
            remove_country_flag = br_skill_combat_stack_unlocked
        }
        else = {
            set_country_flag = br_skill_combat_stack_unlocked
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_energy_overclock = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_energy_overclock_unlocked }
            remove_country_flag = br_skill_energy_overclock_unlocked
        }
        else = {
            set_country_flag = br_skill_energy_overclock_unlocked
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_weakness_scan = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_weakness_scan_unlocked }
            remove_country_flag = br_skill_weakness_scan_unlocked
        }
        else = {
            set_country_flag = br_skill_weakness_scan_unlocked
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_kill_stack = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_kill_stack_unlocked }
            remove_country_flag = br_skill_kill_stack_unlocked
        }
        else = {
            set_country_flag = br_skill_kill_stack_unlocked
        }
    }

    ai_weight = { weight = 0 }
}

br_test_toggle_fullfire = {
    icon = "GFX_edict_type_policy"
    length = 1

    resources = {
        category = edicts
        cost = { }
    }

    potential = { has_country_flag = br_debug_mode }
    allow = { always = yes }

    effect = {
        if = {
            limit = { has_country_flag = br_skill_fullfire_unlocked }
            remove_country_flag = br_skill_fullfire_unlocked
        }
        else = {
            set_country_flag = br_skill_fullfire_unlocked
        }
    }

    ai_weight = { weight = 0 }
}
