br_dominator = {
    # entity = "colossus_entity"
    entity = "juggernaut_core_section_entity"
    # 舰体显示缩放倍率。
    hull_scale = 4
    formation_priority = 4
    max_speed = 60
    acceleration = 0.05 #加速度
    rotation_speed = 0.05 #转向速度
    collision_radius = 32
    max_hitpoints = 10000
    modifier = {
        ship_evasion_add = @colossus_evasion
        starbase_shipyard_capacity_add = 10
        ship_sensor_range_add = 1
    }
    size_multiplier = 32
    map_counter_icon = ship_counter_128
    icon = ship_size_military_64
    fleet_slot_size = 32
    section_slots = {
        "core" = { locator = "part1" }
    }
    num_target_locators = 4
    is_space_station = no
    base_buildtime = @colossus_build_time
    can_have_federation_design = no
    is_designable = yes
    enable_default_design = yes
    enable_3dview_in_ship_browser = no
    can_disable = no
    default_behavior = colossus
    ship_roles = { artillery carrier }
    #不需要科技
    # prerequisites = { "tech_br_dominator" }

    class = shipclass_starbase
    # 不要给母舰启用 is_hero_ship = yes。
    # 母舰必须保留 shipclass_starbase 才能作为船坞建造舰船；英雄舰 UI 会和星基/船坞界面冲突，导致按钮叠到左上角。
    required_component_set = "power_core"
    required_component_set = "ftl_components"
    required_component_set = "thruster_components"
    required_component_set = "combat_computers"
    required_component_set = "sensor_components"
    construction_type = starbase_shipyard

    resources = {
        category = ships
        cost = {
            alloys = 10000
        }
        upkeep = {
            energy = -30
            alloys = -10
        }
    }

    # 建造要求
    potential_country = {
        or = {
            is_country_type = br_player_pve
            is_country_type = br_player_pvp
        }
    }

    min_upgrade_cost = {
        alloys = 100
    }

    ai_ship_data = {
        min = 1
        max = 1
    }

    available_to_everyone = yes
}
