#不能使用巨像以外作战电脑，其他作战电脑无法在战斗中下指令放技能
#is_hero_ship = yes英雄舰
# ============================================================
# 先锋级英雄舰（br_vanguard）
# 使用泰坦模型做英雄舰，等DLC有没有新英雄舰机制用
# 光环槽统一挂载技能
# ============================================================
br_vanguard = {
    entity = "titan_entity"
    # 可被玩家查看
    can_be_inspected = yes
    # 舰体缩放
    hull_scale = 2
    # 航速与机动
    max_speed = 100
    acceleration = 0.1
    rotation_speed = 0.1
    # 碰撞半径
    collision_radius = 8
    # 舰船自身加成
    ship_modifier = {
        ship_weapon_range_mult = 0.5
    }
    modifier = {
    }
    # 基础船体值
    max_hitpoints = 3000
    # 禁止撤退（防止半血逃跑）
    combat_disengage_chance = -1
    # 战斗体积（对方打击倾向）
    combat_size_multiplier = 32
    # 舰队规模，实际舰容占用
    size_multiplier = 32
    # 大地图图标，原版路径interface/solar_system.gfx，似乎是战力的星标，但是并没有生效
    # map_counter_icon = ship_counter_64
    map_icon_override = {
        normal = GFX_fleet_presence_paladin_icons
        mixed = GFX_fleet_presence_paladin_icons_mixed
        cloaked = GFX_fleet_presence_paladin_icons_cloaked
    }
    # 图标，原版路径interface/icons.gfx 舰船类型小图标
    icon = ship_size_military_16
    # 舰队容量占用
    fleet_slot_size = 8
    # 三段式区段，定位器与原版泰坦一致
    section_slots = {
        "bow" = {
            locator = "part1"
        }
        "mid" = {
            locator = "part2"
        }
        "stern" = {
            locator = "part3"
        }
    }
    num_target_locators = 4
    # 英雄舰保护
    never_lost = yes
    never_mia = yes
    is_space_station = no
    on_click_sound = "ui_kaiju_select_fleet"
    #图标原版路径interface/icons.gfx，GFX_ship_class_medium
    ship_class_icon_frame = 61
    #class普通军用可以控制，不过因为is_hero_ship导致没有升级按钮
    # class = shipclass_military_special
    class = shipclass_military
    # 建造属性
    base_buildtime = 60
    can_have_federation_design = no
    enable_default_design = yes
    enable_3dview_in_ship_browser = yes
    construction_type = starbase_shipyard
    # 必需组件集 — 光环槽用于挂载技能
    required_component_set = "power_core"
    required_component_set = "ftl_components"
    required_component_set = "thruster_components"
    required_component_set = "combat_computers"
    required_component_set = "sensor_components"
    required_component_set = "br_hero_ship_Core"
    # 允许设计
    is_designable = yes
    #自动升级 无效，似乎不能允许设计
    # auto_upgrade = yes
    hero_ship = {
        hero_ship_fleet_background = "GFX_fleet_view_main_precom_bg"
        capabilities = {
            can_repair
            can_upgrade
        }
    }
    # 组件计入成本
    components_add_to_cost = no
    # 建造成本,限制一艘，建造用来复活，防止被摧毁后就没了。
    resources = {
        category = ships
        cost = {
            alloys = 1000
        }
    }
    # 建造要求：英雄舰已解锁（BR/PVP/PVE 开局发放或普通模式遗迹局势完成后获得）
    potential_country = {
        has_country_flag = br_hero_ship_unlocked
    }
    # 建造位置
    potential_construction = {
        is_scope_type = starbase
    }
    # 建造条件
    possible_construction = {
        custom_tooltip = {
            fail_text = not_has_br_vanguard_ship
            NOT = {
                any_owned_ship = {
                    is_ship_size = br_vanguard
                }
            }
        }
    }
    min_upgrade_cost = {
        alloys = 100
    }
    available_to_everyone = yes
}
